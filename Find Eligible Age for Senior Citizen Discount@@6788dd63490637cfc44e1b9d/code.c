#include <stdio.h>
int main()
{
    int citizen;
    scanf("%d",&num);
    if(citizen>=60)
    {
        printf("Eligible");
    }
    else
    {
        printf("Not Eligible");
    }
    return 0;
}